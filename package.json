{
  "name": "the-brain",
  "private": true,
  "workspaces": ["packages/core", "packages/adapter-mongo", "packages/adapter-sqlite", "packages/cli", "example-app"],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/adapter-mongo && npm run build -w packages/adapter-sqlite && npm run build -w packages/cli",
    "test": "npm run test -w packages/core && npm run test -w packages/adapter-sqlite && npm run test -w packages/adapter-mongo && npm run test -w packages/cli",
    "dev": "npm run dev -w packages/cli"
  }
}
